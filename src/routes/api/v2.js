/*
import {
  next,
  current,
  year
} from '../../controller/v2';

const BASE = '/api/v2';

export default [
  { method: 'GET', path: `${BASE}/feriados/proximo`, config: next },
  { method: 'GET', path: `${BASE}/feriados/actual`, config: current },
  { method: 'GET', path: `${BASE}/feriados/{year}/{month}`, config: year }

  //{ method: 'GET', path: `${BASE}/festivos/actual`, config: current },
  //{ method: 'GET', path: `${BASE}/festivos/{year}`, config: next },
  //{ method: 'GET', path: `${BASE}/festivos/{year}/{month}`, config: year }
];
*/
